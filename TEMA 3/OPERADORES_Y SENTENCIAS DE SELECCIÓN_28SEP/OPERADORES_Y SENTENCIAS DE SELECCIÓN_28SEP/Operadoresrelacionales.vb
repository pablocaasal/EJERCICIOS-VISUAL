Module Operadoresrelacionales
    Sub Main()
        'Operadores relacionales (devuelven un valor de tipo boolean)
        ' <, > , <=, >= , = , <> 
        Dim x As Integer = 9
        Dim y As Integer = 9

        Dim condición1 As Boolean = x < y
        Dim condicion2 As Boolean = x >= y

        Dim Condicion3 As Boolean = x = y
        Dim CONDICION4 As Boolean = Condicion3 <> True
    End Sub
End Module
